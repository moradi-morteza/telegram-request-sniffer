# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
# Use '*' to allow all origins (development only), or specify your frontend URL
CORS_ORIGIN=*

# WebSocket Configuration
WS_HOST=localhost

# Logging
LOG_LEVEL=info
